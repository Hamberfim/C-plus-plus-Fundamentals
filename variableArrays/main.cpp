#include <iostreamn>
using namespace std;

int main()
{
    // using variable arrays
    // declared and initialized
    int numbers[4] = {0, 1, 2, 3};

    //declared and initialized
    float flts[3];
    flts[0] = 1.2;
    flts[1] = 1.7;
    flts[2] = 2.1;



    // character array - used less than the string class
    char myName[8] = {'A', 'n', 't', 'h', 'o', 'n', 'y', '\0'};



    return 0;
}
